exp_name: 'test'
save_path: 'prediction'
remarks: "prediction of what model on what data"
device: 'cuda:0'  # make it consistent throughout all codes. gpu index or cuda/cpu
# backup: ['code', 'config']  # not yet implemented

predict:
  model_ckpt: '/path/to/finalcumodel.pt'  #if you ever moved the .pt file, make sure there is a 'config.yaml' in the parent directory of .pt file
  csv_path: '/path/to/energy.csv'
  label_column: 'label_e'
  pred_column: 'PRED_E_covalent'
  loading_workers: 8
  batch_size: 500
  dataset_name: "Metal"
  h5_files: ['/path/to/data.h5'
  ]  # store xyz data
  h5_multipole: ""
